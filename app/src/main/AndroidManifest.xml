<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ibdaatest">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IbdaaTest">
        <activity android:name=".ui.SharedPrefActivity"></activity>
        <activity android:name=".ui.Customview" />
        <activity
            android:name=".Recyclerfromlocal.Recyclerlist"
            android:label="@string/title_activity_recyclerlist"
            android:theme="@style/Theme.IbdaaTest.NoActionBar" />
        <activity
            android:name=".LanuchActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.IbdaaTest.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyNewsApp.NewsActivity" />
        <activity android:name=".MyNewsApp.SettingsActivity" />
    </application>

</manifest>